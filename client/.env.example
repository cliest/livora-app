# Copy to .env.local for a production build pointed at a deployed API.
# Leave unset in local dev — the Vite dev server proxies /api/* to
# http://localhost:4000 automatically (see vite.config.js).
VITE_API_URL=
